<% content_for :nav_content do %>
  <div class="btn btn--faux">
    <span>All Talk</span>
  </div>
<% end %>

<div class="messages" style="align-items: center; justify-content: center;">
  <div style="text-align: center; padding: 2em; max-width: 50ch;">
    <div style="font-size: 4em; margin-bottom: 0.5em;">🔥</div>
    <h1 style="margin: 0; font-size: 2em; margin-bottom: 0.5em;">Welcome to OneRev</h1>
    <p style="color: var(--color-border-darker); font-size: 1.1em; line-height: 1.6;">
      To invite people to chat, share the join link below.
    </p>

    <% if @rooms.empty? %>
      <div style="margin-top: 2em; padding: 1.5em; background: var(--color-border); border-radius: 1em; border: 1px solid var(--color-border-dark);">
        <p style="margin: 0; color: var(--color-text); font-weight: 500;">
          You don't have any rooms yet.<br>
          Create your first room or ping someone to get started!
        </p>
        <div style="display: flex; gap: 1em; justify-content: center; margin-top: 1em;">
          <%= link_to "Create Room", new_room_path, class: "btn btn--reversed" %>
          <%= link_to "Ping Someone", new_direct_path, class: "btn" %>
        </div>
      </div>
    <% else %>
      <div style="margin-top: 2em;">
        <p style="color: var(--color-text); font-weight: 500;">
          Select a room from the sidebar to start chatting →
        </p>
      </div>
    <% end %>
  </div>
</div>
